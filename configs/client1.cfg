delete / acl
delete / system control-plane-traffic
set / network-instance default admin-state enable
set / network-instance default interface ethernet-1/1.10
set / network-instance default interface system0.0
set / network-instance default static-routes route 0.0.0.0/0 admin-state enable
set / network-instance default static-routes route 0.0.0.0/0 next-hop-group srl-ntp-server
set / network-instance default next-hop-groups group srl-ntp-server admin-state enable
set / network-instance default next-hop-groups group srl-ntp-server nexthop 1 ip-address 40.40.40.1
set / network-instance default next-hop-groups group srl-ntp-server nexthop 1 admin-state enable
set / system ntp admin-state enable
set / system ntp network-instance default
set / system ntp server 1.1.1.1 iburst true
set / system ntp server 1.1.1.1 prefer true
set / interface ethernet-1/1 admin-state enable
set / interface ethernet-1/1 vlan-tagging true
set / interface ethernet-1/1 subinterface 10 ipv4 admin-state enable
set / interface ethernet-1/1 subinterface 10 ipv4 address 40.40.40.40/24
set / interface ethernet-1/1 subinterface 10 vlan encap single-tagged vlan-id 10
set / interface system0 subinterface 0 ipv4 admin-state enable
set / interface system0 subinterface 0 ipv4 address 5.5.5.5/32
